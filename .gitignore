# real machine data (scan results, inventories, backups) — private, never commit
/scans/
/backups/

# node
node_modules/
.next/
dist/

# env / secrets
.env
.env.*
!.env.example

# misc
.DS_Store
.idea/
*.log
