# real machine data (scan results, inventories, backups) — private, never commit
/scans/
/backups/

# app data (SQLite DB) — private, never commit
/data/

# node
node_modules/
.next/
dist/
/src/generated/

# env / secrets
.env
.env.*
!.env.example

# misc
.DS_Store
.idea/
*.log
